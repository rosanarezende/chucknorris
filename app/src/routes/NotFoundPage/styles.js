import styled from "styled-components"

export const NFWrapper = styled.div`
    height: 90vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
`

export const NFLink = styled.div`
    cursor: pointer;
    margin: 1rem 0;
    display: flex;
    align-items: center;
`